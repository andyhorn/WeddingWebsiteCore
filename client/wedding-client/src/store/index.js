import Vue from 'vue'
import Vuex from 'vuex'
import { http, addToken, stripToken } from "@/axios";

Vue.use(Vuex);

const defaultState = {

};

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
